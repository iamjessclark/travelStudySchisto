# [Content removed during history cleanup: deprecated script that
# contained a hardcoded Google Maps API key. Superseded by
# Maps_open.R, which needs no API key and reads coordinates from a
# gitignored private_data/ file.]
